-- Shared utility functions used across tables

-- Function to auto-update updated_at column on row changes
create or replace function update_updated_at_column()
returns trigger as $$
begin
  new.updated_at = now();
  return new;
end;
$$ language plpgsql;

